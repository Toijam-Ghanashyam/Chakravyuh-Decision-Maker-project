// Template config for client-side usage
// Copy to `config.js` and fill in real values, or use `.env` + `npm run gen-config` to generate
const CONFIG = {
  GEMINI_API_KEY: 'YOUR_GEMINI_API_KEY_HERE',
};
